# Phase 3 exploration — gate opening and upstream-acceptance machinery

Read-only exploration of the current tree (post-phase-2, `f149e27` + staged durable-state records). Line references verified 2026-08-19.

## Per-site current state

**1. composeGate — src/state/request-composition.ts:509-731.** Signature `composeGate(services, state, intentId, snapshot): Promise<ProjectResult<CallEnvelope>>`. Gate-kind derivation at 547-552: `phase-impl → commit-authorization`, `design|phase-design → design-approval`, else `artifact-approval` — purely from phase kind, no rule input. Composition order: baseline-adoption (519-545) → exhaustion → migration-audit (625-657) → design-approval (680-691) composes AHEAD of the pending constitution arm (692-701): for design/phase-design the combined gate wins because its policy arm discharges constitution-review; `buildDesignApprovalInput` (src/state/status.ts:675-709) embeds `designApprovalPolicyContext(retained adjudication)` = {constitution: pass, policy_findings, eligible_waivers} plus commit facts. Disagreement guard at 588-619: try/catch around the exact status-mirror computation — `read_config → parseConfigYaml → max_attempts`, `currentApprovedUpstreams(...)`, `currentReviewPredecessor`, `assessCurrentEvidence(...)` — any throw returns `TRANSITION_INVALID` to `gate-fixed-point-disagreement`; `assessment.next === "attempts-exhausted"` sets the exhaustion gate. Seam: 547-552 is the single decision point a rule match must intercept; parsed config flows in at 596, but changed file paths are NOT in hand here (only composeProduce reads `readChangedGitPaths`, line 240).

**2. fixed-point — src/review/fixed-point.ts.** `adjudicationGateSatisfied` (332-375): for design/phase-design constitution-review gates, satisfied by an authenticated design-approval whose `approval.gate_kind==="design-approval"`, subject digests match, `request.current_evidence.set_digest === deriveCurrentEvidenceSet(retained).set_digest`, decision `"approve"` (340-349), or a migration-audit `"accept-import-audit"` (352-361); otherwise the full `gateApprovalBindingFailure` chain (199-313) or waiver coverage (320-330). `resolveAdjudicationGateStep` (502-519): first unsatisfied gate from `selectAdjudicationGates(constitution.rules, adjudication)` → `"adjudication-gate"`. `currentFor` "adjudicate" (173-180): retained adjudication stays current only while `adjudication.approved_upstream_digests` equals the caller-supplied list element-wise. `requireApprovedUpstreamDigests` (586-602): `(approvals, upstreamDigests) => readonly Sha256Digest[]`; sorts input, throws `TypeError` on dupes / `upstream ${digest} lacks current document approval` unless some ApprovalRef has `gate_kind` artifact-approval|design-approval with matching `subject_digest` — a shallow durable-ref scan, no archive authentication, no restart filter. Seam: the gate-kind pair at 596-597 and the design/migration branches at 340-362.

**3. currentApprovedUpstreams — src/state/status.ts:487-528.** `(dependencies, authority, state, authenticated, subject) → readonly Sha256Digest[]` (sorted set). Bindings from `expectedProduceUpstreamBindings(state)` or `produceUpstreamBindingsForSubject` (co-produced docs excluded); per binding `loadProduceUpstreamSubject`, then imported arm (design phase or accepted migration-audit) or an authenticated approval with `gate_kind design-approval|artifact-approval`, matching `request.kind` and `request.context.artifact_kind === ownerKind`, restart-eligible, latest `resolved_at_revision` wins; throws `TypeError("current upstream produced authority lacks approval")`. Callers: status (1028, wrapped by `resolveStatusEvidenceAssessment` 530-548 → blockers `approved-upstream-authority-unavailable` / `fixed-point-disagreement`) and composeGate (600, inside the guard). Seam: the `approval === undefined` throw at 524.

**4. deriveApprovedUpstreams — src/mcp/handlers/counter-review.ts:122-203.** Per binding: load upstream, read/re-hash projections, render text; `approved` = imported-in-design, or authenticated migration-audit naming exact path+content_digest (154-172), or authenticated artifact/design approval with matching `request.context.artifact_kind` (173-192, sorted desc by `resolved_at_revision`). Failure: `STATE_INVALID "upstream-approval-missing"` (193-197). Called at handleCounterReview:347 when constitution rules are active; digests pass through `requireApprovedUpstreamDigests` (349-352). Seam: the `!approved` branch at 193.

**5. assembleUpstreamContext — src/review/pinned-context.ts:250-305.** Approval check (261-266) is the shallowest: imported → design phase or *any* migration-audit ApprovalRef; else `state.approvals.some(gate_kind artifact-approval|design-approval && subject_digest === upstream.artifact_digest)` — no authentication, no restart filter, no artifact-kind match. Failure `STATE_INVALID "upstream-approval-missing"` (266); emits `approved-upstream` pinned entries. Can pass where the authenticated scanners fail.

**6. loadProduceUpstreamSubject — src/state/produce-subject.ts:82-175.** The shared chokepoint for 3/4/5. Walks `state.authoritative_results` reversed (produce step), requires a document artifact that owns `binding.path`, digest-matches, AND (110-118) `state.approvals.some(artifact-approval|design-approval && subject_digest === manifest.artifact_digest && approvalIsEligibleAfterLatestRestart(state, approval, artifact.phase_instance))`. Unapproved retained owner → `STATE_INVALID "upstream-approval-missing"` (142); no owner → legacy-import imported_projection path (143-174). Highest-leverage seam: one receipt acceptance here feeds three consumers, though each re-checks approvals itself.

**7. planned_final_phase — src/state/planned-final-phase.ts.** `plannedFinalPhaseFromDesign` (9-47, marker/heading grammar), `loadApprovedDesignFinalPhase` (50-87): only a decided `artifact-approval|design-approval` record on phase `design` with decision `"approve"`; re-authenticates the retained design.md produce result against `record.subject_digest` and re-hashes the payload. Failures: `approved-design-result-missing`, `approved-design-authority-mismatch`, `approved-design-phase-count-invalid`. Called from gate resolution at src/state/gates.ts:763, 789, 820-830. Seam: the record-kind/decision filter at 55-60.

**8. Approvals writes + archive.** `ApprovalRef` (src/contracts/durable-state.ts:64-71): `{gate_id, gate_kind, subject_digest, decision_digest, resolved_at_revision}` (schema 584-585, sorted-unique by gate_id). Written only in `nextStateForRecord` (src/state/gates.ts:469-492) when `record.outcome === "decided" && gateDecisionEffect(payload) === "advance"`. Archive: `.archflow/tasks/<task>/authority/decisions/<gate_id>/{request.json,decision.json}` (src/repository/paths.ts:108-119); `authority/initialization.json`, `authority/results/<digest>.json`. `loadAuthenticatedGateApproval` (src/state/gate-approvals.ts:54-137) re-reads state + both files, deep-equals the ref, validates semantics, mints a branded capability; failures `gate-approval-not-current` etc.

**Config reality:** src/contracts/config.ts:35-40 — only `schema_version`, `roles`, `overrides` (per phase kind), `max_attempts`. Changed file paths available via `readChangedGitPaths` (src/repository/git.ts:318) but currently only used in composeProduce.

## Additional sites the task design's list missed

- **src/state/next-action.ts:138-175** — `matchingApproval`/`advanceAction`: status routing requires a matching approval fact (design→design-approval, prd→artifact-approval, phase-impl→commit-authorization, migration-audit for imports) else advertises `open-gate`. The rule-conditional advance must surface here or composeAdvance (request-composition.ts:739-762, which trusts `computeTaskStatus().next_action`) refuses.
- **src/state/transitions.ts:197-225, 253-266, 397-422** — `hasAuthenticatedArtifactApproval`, `hasAuthenticatedCombinedDesignApproval`, `hasAuthenticatedMigrationAudit`, `hasAuthenticatedCommittedOutput`; phase-exit moves in `legalMovement` (298-309) require them.
- **src/mcp/handlers/state.ts:390-560** — completion/phase-exit/legacy-jump signals scan `current.value.approvals`, authenticate, and feed `planStateTransition` (triage settle at :518).
- **src/state/gates.ts:384-391** — migration-audit opening requires `traditionallyApproved` artifact/design approval or reviewed migration (import path; exception gate).
- **src/state/restart-authority.ts:26-52** — `approvalIsEligibleAfterLatestRestart`, the eligibility filter consumers apply; receipts must define their own eligibility semantics.
- Minor: workspace-cleanup.ts:224 (retains decisions dirs), durable.ts:733 (baseline-adoption semantics), transaction.ts:333 (approvals deep-equal invariant), semantic-view.ts:248-254 + semantic-actions.ts:318/383/580 (semantic `gate-summary` routes into composeGate).

## Tests pinning each site

1. composeGate: test/integration/semantic-composition-parity.test.ts, test/unit/local-build-request.test.ts, test/integration/mcp-handler-state-replay.test.ts, test/integration/planning-restart-handler.test.ts, planning-restart-replay.test.ts, test/integration/status-reentry-edit.test.ts. (`gate-fixed-point-disagreement` itself untested directly.)
2. fixed-point: test/unit/review-services.test.ts (293-395, 461-628, 729-730), test/integration/review-fixed-point.test.ts, review-fixed-point-live.test.ts, test/unit/state-gates.test.ts.
3. currentApprovedUpstreams: test/unit/state-status.test.ts:136; test/unit/review-services.test.ts:378.
4. deriveApprovedUpstreams: test/integration/counter-review-pinned-context.test.ts:399-406 (`upstream-approval-missing`).
5. assembleUpstreamContext: same integration file (:371); test/unit/pinned-context.test.ts pins entry mechanics only.
6. loadProduceUpstreamSubject: test/unit/state-produce-subject.test.ts + the integration file above.
7. planned_final_phase: test/unit/planned-final-phase.test.ts, test/contracts/task-state-planned-final-phase.test.ts, test/unit/state-gates.test.ts:371, test/unit/state-transitions.test.ts:535.
8. Approvals/archive: test/integration/state-gate-lifecycle.test.ts (366, 406, 417, 432, 1086), test/unit/state-gates.test.ts, test/unit/durable-state.test.ts, test/contracts/durable-gate(-validation|-v1-compatibility).test.ts.
Missed sites: test/unit/state-next-action.test.ts (open-gate routing), test/unit/state-transitions.test.ts:97/114 (approval-required phase exit), test/unit/semantic-actions.test.ts + semantic-view.test.ts (semantic surface).
